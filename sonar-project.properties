# SonarCloud configuration for tondays52_CredX.
# projectKey / organization are supplied by the SonarCloud GitHub App workflow.

sonar.sources=.
sonar.exclusions=**/extensions/**,**/.github/**,**/node_modules/**,**/dist/**,**/artifacts/**,**/cache/**,**/demo-assets/**,**/coverage/**,**/typechain*/**,**/out/**,**/build/**

# S2245 (weak pseudo-random source): all occurrences are NON-security randomness —
# three.js star fields, cosmetic chart jitter, toast ids, and the explicitly labeled
# local lootbox/telemetry demo generators. No security-sensitive RNG (keys, nonces,
# signatures, seeds) is ever derived from Math.random() in this codebase.
sonar.issue.ignore.multicriteria.r1.ruleKey=javascript:S2245
sonar.issue.ignore.multicriteria.r1.resourceKey=**
sonar.issue.ignore.multicriteria.r2.ruleKey=typescript:S2245
sonar.issue.ignore.multicriteria.r2.resourceKey=**